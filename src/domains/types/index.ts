/**
 * A semantic alias for bigint values ​​in the token domain. It improves readability and clearly distinguishes token values ​​from other bigint values. In fact, this is the type of internal representation of a token.
 */
export type Atomic = bigint;